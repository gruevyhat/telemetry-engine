# @telemetry/plugin-traveller

Fair Use Policy layer for the Traveller setting (Spec §1). This package implements
`packages/engine`'s plugin API against Traveller-specific terms and content; the engine
itself contains zero setting content and zero Traveller terms (INV-1).

The Traveller game in all forms is owned by Mongoose Publishing. Copyright 1977 - 2024 Mongoose
Publishing. See `NOTICE.traveller` at the repository root for the full Mongoose Fair Use
disclaimer required by their policy's FAQ item 1(b).

Per FAQ item 1(c), Mongoose Publishing must also be notified directly by email
(sales@mongoosepublishing.com) that this site exists. That notification is an owner action
(`docs/tasks/M0-00.md`) — check that card for whether it has been sent yet.
