# M0-07 handoff - session 2

## Branch state
`milestone/M0`, with M0-07 still uncommitted in the working tree. This session continued from `docs/handoffs/M0-07-1.md`.

Unrelated untracked items still present and intentionally untouched: `.claude/`, `.codex/`, `docs/img/`.

## What changed
- Unblocked root Vitest `.tsx` tests by removing the unused root React plugin and adding Vitest resolver aliases that point UI tests at `packages/ui-shared/node_modules` under pnpm's isolated layout.
- Added `PhaseTrack.test.tsx` for the four-turn-beat rule and the live-beat indicator.
- Added `App.test.tsx` to assert the runnable shared-screen shell plus hand-to-player interstitial.
- Wired `packages/ui-shared/src/App.tsx` to a deterministic demo ledger using real `@telemetry/engine` exports and projections.
- Added minimal shared-screen region styling for status bar, phase track, main panel, and ticker while keeping the type floor at 20px or higher.
- Kept `jsdom` as a root dev dependency for Vitest's jsdom environment and added `@testing-library/react` as a `packages/ui-shared` dev dependency.

## Validation
- `npx vitest run` - passed, 19 files / 63 tests.
- `npx eslint .` - passed.
- `node scripts/build-stub.mjs` - passed.
- `npx tsc --noEmit -p packages/ui-shared/tsconfig.json` - passed.
- `npx tsc --noEmit -p packages/engine/tsconfig.json` - passed.
- `./node_modules/.bin/vite build` from `packages/ui-shared` - passed. Generated `packages/ui-shared/dist/`, which is ignored.

`pnpm` and `corepack` are not available on this shell's PATH, so the standard command wrappers could not be invoked directly.

## Red/green status
No red or green commit was created in this session. The code is green locally, but commit creation should be done explicitly by the owner or by a follow-up agent after approval to commit. To reconstruct the repo's expected red/green sequence, commit the failing tests first by temporarily stubbing the relevant component behavior, then restore this green implementation and commit the implementation.

## Decisions and Spec basis
- `PhaseTrack` renders only `DOCKSIDE`, `COMMS`, `TRANSIT`, and `ARRIVAL`, following rulebook section 3.1 and section 6's "four beats" phrasing.
- `Ticker` keeps filtering to `visibility.level === "public"` so private, table, and referee facts cannot appear in the bottom journal.
- `Interstitial` still receives an already scoped `visibleFacts` slice; INV-13 is enforced by the caller using `ledger.visibleTo({ scope: "private", playerId })`.
- The app fixture uses real M0 ledger/projection APIs so the status bar path exercises the engine barrel added in `packages/engine/src/index.ts`.

## Exact next action
If commits are desired, ask for commit approval, then stage only the M0-07 files and create the repo-required red and green commits. Otherwise the implementation is ready for review in the working tree.
