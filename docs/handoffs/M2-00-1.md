# Handoff: M2-00 (session 1)

## Branch state

`milestone/M2` in the isolated worktree `/private/tmp/telemetry-engine-m2`, based on merged M1. The original checkout's modified rulebook/spec/catalog/art files remain untouched. M2 planning commit `520c2e6` is pushed and green.

M2-00 now has a committed design draft plus follow-up review revisions. The revisions specify rotatable phone-binding epochs, an engine-owned authorized presentation projector, agenda widening on burn, durable timer checkpoints, finite agenda target kinds, a complete message inventory, fixed ballot mechanics, accessibility acceptance, and a recovery-wrapped campaign seed. No code, dependency, runtime schema, or Visibility behavior changed.

## Red/green status

M2-00 is a design-only card, so TDD is not applicable. `git diff --check` is clean. The draft was manually reviewed against Spec §§3.3/6/10.2/16/21.3, rulebook §8.2, the fact-kind catalog, the current persistence loader, and Appendix A.

## Decisions awaiting owner approval

1. Declarative `AgendaActionContent` with a conjunctive optional target selector, closed template references, proposals, implication metadata, payout, and exposure.
2. `agenda.actionTaken` means durable queued intent; add catalog-first kinds for objective assignment, deferred-reveal mint/cash, captain assignment, and crew removal; use `confrontation.resolved` for let-it-lie.
3. Add trystero in `packages/transport-webrtc` and qrcode plus types in `packages/ui-shared`; nothing new in `packages/engine`.
4. Save schema v2 with one-way M1 migration; persist replay/session bindings, not live peers, QR tokens, or phone keys; phone reload requires re-pairing.
5. Package split: protocol/crypto in `packages/transport`, trystero adapter in `packages/transport-webrtc`, only the `ui-shared` host bridge can accept a Ledger, phones remain DTO-only.
6. Confrontation votes are public as cast through cumulative append-only `vote.recorded` facts; COMMS actions remain sealed until close.

The owner confirmed the 2026-07-18 forced-vote decision: envelopes open only after a carried majority vote, never voluntarily. The authoritative Spec, rulebook, fact-catalog, and Appendix A edits were then ported to the M2 branch as the prerequisite for M2-07.

The owner also approved strengthening commit/reveal on 2026-07-18. `te-commit-v1` adds a setup-time campaign-seed commitment and binds each draw's canonical result, stream, index, salt, and seed-commitment hash. Campaign-end verification checks the seed commitment, re-derived RNG result, and draw hash independently.

The owner approved all six remaining M2-00 recommendations together on 2026-07-18. `screens-v2.md` is now the approved implementation basis, and the authorized social fact kinds/payload revisions have landed catalog-first.

## Extrapolations

- Pairing QR is private behind the existing hand-to interstitial because it carries a per-player key.
- A normal disconnect retains an in-memory phone key; a page reload loses it and requires re-pairing.
- Client `playerId` is never authority; the host uses the paired connection binding and rejects mismatches.
- Public-as-cast confrontation votes follow the existing sim policy's `majority-so-far` rule. COMMS simultaneity remains separate and sealed.

## Exact next action

Commit and push the completed M2-00 decision record, run branch CI, then begin M2-01 by reading its card and referenced Spec sections and writing the failing commit/reveal tests first.
