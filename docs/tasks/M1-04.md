---
id: M1-04
title: Validator passes 1–4 + implies closure engine
assignee: any implementer
spec_refs: ['Spec §9', 'fact-kinds §1–3']
invariants: ['INV-5 (partial)']
---

# M1-04 — Validator passes 1–4 + implies closure engine

## Tests first
- corpus: planted contradictions (schema, dangling ref, unreachable actor, timeline) — all rejected with typed reasons
- unit: implies closure over the catalog matches hand-computed closure for The Skim (fact-kinds §3 example)
- property: closure is monotone and terminates on random fact sets

## Done when
- validate(bundle, ledgerView) pure function; consistent-worlds enumerator (brute force, actors ≤ ~10)

## Do not
- Ambiguity *rejection* wiring (pass 5) lands in M1-05 with frames; this card builds the enumerator
- Do not build a SAT solver (fact-kinds §3)
