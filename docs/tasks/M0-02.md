---
id: M0-02
title: Ledger core: Fact types, kind registry, append, ordering
assignee: any implementer
spec_refs: ['Spec §2', 'docs/design/fact-kinds-v0.md']
invariants: ['INV-2']
---

# M0-02 — Ledger core: Fact types, kind registry, append, ordering

## Tests first
- property: append-only — no API mutates or removes; correction supersedes in projections
- property: ulid ordering is total and time-consistent
- unit: kind registry rejects unregistered kinds and malformed payloads (schema per catalog)
- unit: split-visibility rule — registering a kind with mixed-visibility payload fields is impossible by construction

## Done when
- Fact, Visibility, ActorRef types per Spec §2.1
- registry loads the v0 catalog kinds
- append + read API with visibility-filtered views

## Do not
- Do not implement reducers here (M0-03)
- Do not implement implies/closure here (M1-04)
