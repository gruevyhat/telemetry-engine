---
id: M1-14
title: Skill-check UI wired into the trade campaign (transit/dockside checks)
assignee: any implementer
spec_refs: ['Spec §6', 'Spec §21.3 (M1 acceptance)', 'docs/design/screens-v1.md']
invariants: []
---

# M1-14 — Skill-check UI wired into the trade campaign

## Context

Follow-on to M1-13 (which wired the real market/incident loop into `App.tsx` but explicitly kept
checks out of scope). `createPhaseInterpreter`'s `"check"` step kind already works end to end:
`resolveStep`'s `"check"` case reads `input.checkTotal` and branches to `onSuccess`/`onFail`
(`packages/engine/src/phases/interpreter.ts`), and `PhaseInterpreter.advance(t, actor, input)`
already accepts `{ checkTotal }`. This is UI-only work — no interpreter change needed. Spec §6:
"the engine never rolls for a PC," so the player enters their own roll result; nothing here
generates a number.

## Tests first
- `App.test.tsx`: a check step in the trade-campaign script renders a roll-entry control; entering
  a value at/above the step's `difficulty` and confirming takes the `onSuccess` branch, below it
  takes `onFail`; both branches are asserted via the resulting step/announcement, not by reaching
  into interpreter internals
- unit (`interpreter.test.ts`, if not already covered): confirm the existing `"check"` case's
  behavior — it does not itself commit any fact, only branches; decide in this card whether that
  gap (no `check.reported` fact logged) is in scope here or a separate follow-on, and record which
  in this card's Done-when before writing UI around it

## Done when
- at least one check step added to `content/frames/trade-campaign/turn.json` (a plausible spot:
  the TRANSIT jump, replacing its literal `jump.plotted`-only announce with a real
  difficulty/onSuccess/onFail branch) with a corresponding UI control for entering a roll total
- `App.tsx` renders a minimal roll-entry control (a number input + confirm) when the current step
  is `"check"`, calls `advance(t, actor, { checkTotal })`, and shows the resulting branch
- `App.test.tsx`, `tests/integration/trade-campaign.test.tsx`, and `scripts/pages-smoke.mjs` are
  updated for the script's new (longer) advance count — same discipline M1-13 used when it first
  wired the campaign in

## Do not
- No dice-rolling in the engine or the UI — a plain number input for the player's own roll total
  is sufficient; do not simulate a die
- Do not add `check.reported` fact-logging unless this card's Done-when says so explicitly (see
  Tests-first) — if left out, card that gap for a follow-on rather than silently leaving it undone
- Do not touch `Visibility` handling
