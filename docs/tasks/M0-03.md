---
id: M0-03
title: Reducers and memoized projections: funds, positions, clocks
assignee: any implementer
spec_refs: ['Spec §2', 'Spec §5', 'fact-kinds §2 position model']
invariants: ['INV-3', 'INV-7']
---

# M0-03 — Reducers and memoized projections: funds, positions, clocks

## Tests first
- property: replay determinism — derive(facts) byte-identical across 100 seeds and across two runs
- property: clock projection equals sum of its clock.tick facts
- unit: correction supersession affects projections correctly
- unit: presence projection — absent declaration resolves to berth/common

## Done when
- pure reducer framework + memoization
- projections: funds, per-actor presence, clocks
- projection versioning field wired to schemaVersion

## Do not
- No IO in reducers
- Do not add projections speculatively; only the three named
