---
id: M1-01
title: Economy: market.tick reducer, drift, marketAt(hex, day)
assignee: any implementer
spec_refs: ['Spec §7']
invariants: ['INV-9']
---

# M1-01 — Economy: market.tick reducer, drift, marketAt(hex, day)

## Tests first
- property (time-travel): feed answer for hex H at distance d equals marketAt(H, day−7d) exactly, over random walks
- unit: mean reversion bounds; shock application from world-event facts
- unit: active-bubble rule — no ticks generated outside reachable-staleness hexes

## Done when
- weekly tick generation on stream market:<hex>; marketAt projection; plugin goods list consumed via plugin-api

## Do not
- No elasticity/order books (Spec §7 Do-not); supply response term stubbed until M1-11 tunes it
