---
id: M0-06
title: Phase interpreter: step kinds, transitions-as-facts, resume
assignee: any implementer
spec_refs: ['Spec §4', 'Spec §3.2', 'Spec §3.3 (queue hook only)']
invariants: ['INV-6']
---

# M0-06 — Phase interpreter: step kinds, transitions-as-facts, resume

## Tests first
- integration: scripted fixture turn runs DOCKSIDE→ARRIVAL emitting expected fact sequence
- integration: kill-and-resume mid-step equals uninterrupted run (state + facts identical)
- unit: exactly-one-active-step invariant under malformed scripts (rejected at load)

## Done when
- interpreter for all Spec §4 step kinds except confrontation sub-script (M2)
- commsWindow step queues actions but batch-resolution is a stub returning empty (M2 fills it)
- single-writer: the only ledger.append call site in the codebase

## Do not
- Do not implement vote UI, timers UI (M0-07), or generation (M1-03+); steps may call stubs
