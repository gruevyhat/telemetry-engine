---
id: M0-08
title: Save/load/export blob
assignee: any implementer
spec_refs: ['Spec §18']
invariants: ['INV-3']
---

# M0-08 — Save/load/export blob

## Tests first
- property: load(export(state)) replays byte-identical across 50 random fact streams
- unit: contentHashes mismatch surfaces the warn-and-replay-validate path
- unit: schemaVersion mismatch refuses load with the specified message

## Done when
- IndexedDB autosave via idb; JSON export/import; hash pinning of content files

## Do not
- No migration logic beyond refuse/warn (Spec §18); no cloud anything
