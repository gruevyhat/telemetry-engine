---
id: M0-01
title: Repo scaffold, packages, CI skeleton, stub plugin, lint rules
assignee: any implementer
spec_refs: ['Spec §1', 'Spec §21.1']
invariants: ['INV-1', 'INV-6']
---

# M0-01 — Repo scaffold, packages, CI skeleton, stub plugin, lint rules

## Tests first
- stub-plugin build job passes with plugin-traveller absent
- eslint rule test: a planted ledger.append outside phases/ fails lint
- eslint rule test: a planted Math.random in packages/engine fails lint
- CI rejects a PR whose description lacks a required template section (script test)

## Done when
- pnpm workspace with packages per Spec §1 layout
- all §3 Plan commands exist and run (may be trivial)
- GitHub Actions: PR gate wired (test, lint, build:stub)
- PR template installed as .github/pull_request_template.md

## Do not
- No application logic in this task; skeleton only
- Do not add dependencies beyond: typescript, vite, vitest, fast-check, eslint, ulid, idb, react (ui packages only)
