---
id: M2-00
title: Social-game screen, transport, and owner-decision map
assignee: owner+agent
spec_refs: ['Spec §3.3', 'Spec §6', 'Spec §10.2', 'Spec §16', 'rulebook §8.2']
invariants: []
---

# M2-00 — Social-game screen, transport, and owner-decision map

## Context
M2 joins secret agenda mechanics, two UIs, transport, timers, confrontation, and cryptographic audit. Before code, record their boundaries in `docs/design/screens-v2.md` and close the decisions below. The owner's uncommitted 2026-07-18 rulebook/spec/catalog edits already decide that an envelope is burned only by a carried majority confrontation vote, never voluntarily; those authoritative edits must land before M2-07.

## Tests first
- n/a — design task; review every message and screen against its visibility slice and Appendix A

## Owner decisions required
- approve a declarative `AgendaAction` content shape (access precondition, proposal bundle, claimant id, payout/exposure metadata) before M2-03 changes a schema
- complete a social fact-kind audit before code: decide whether `agenda.actionTaken` is the durable queued intent or a new catalog kind is required, catalog the private-objective/deferred-reveal representation M2-07 needs, and settle captain-replacement/put-off-ship/let-lie consequences for M2-07b
- approve UI dependencies for trystero and QR encoding before M2-11; no dependency enters `packages/engine`
- approve the encrypted-save schema version/migration before M2-10 and decide which non-secret pairing/session metadata persists across reload; secret salts/keys must satisfy §16 without claiming host-proofness
- choose the package boundary for transport protocol code so neither UI package becomes an accidental full-ledger client

## Done when
- screens v2 maps setup/deal, phone pairing, timed COMMS, private feedback, confrontation vote, disconnect fallback, and black-box verification
- each host/client message lists sender, receiver, visibility, encryption boundary, replay/idempotency rule, and disconnect behavior
- decisions above, any required fact-catalog-first commits, and the forced-vote prerequisite are recorded in this card

## Do not
- no code, dependency, schema, or Visibility change in this card
- do not design spoken-play capture or imply that encryption defeats a malicious host
