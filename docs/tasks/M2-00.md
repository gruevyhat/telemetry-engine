---
id: M2-00
title: Social-game screen, transport, and owner-decision map
assignee: owner+agent
spec_refs: ['Spec §3.3', 'Spec §6', 'Spec §10.2', 'Spec §16', 'rulebook §8.2']
invariants: []
---

# M2-00 — Social-game screen, transport, and owner-decision map

## Context
M2 joins secret agenda mechanics, two UIs, transport, timers, confrontation, and cryptographic audit. Before code, record their boundaries in `docs/design/screens-v2.md` and close the decisions below. The owner's uncommitted 2026-07-18 rulebook/spec/catalog edits already decide that an envelope is burned only by a carried majority confrontation vote, never voluntarily; those authoritative edits must land before M2-07.

## Tests first
- n/a — design task; review every message and screen against its visibility slice and Appendix A

## Owner decisions required
- **Agenda content — approval pending.** Recommend the declarative shape in screens-v2 §2.4: id + template key, access precondition, optional conjunctive target selector, fact-proposal templates with a closed value-reference vocabulary, action-specific implication metadata, and numeric payout/exposure metadata. Incident frames continue to claim by `agendaActionId`; no executable content callbacks.
- **Durable queue and fact catalog — approval pending.** Recommend `agenda.actionTaken` mean queued intent, committed at selection and rehydrated by pinned content id at close. Add catalog-first kinds for private objective assignment, deferred-reveal mint/cash, captain assignment, and crew removal; use existing `confrontation.resolved` for let-it-lie.
- **UI dependencies — approval pending.** Recommend Spec-prescribed `trystero` in a WebRTC adapter package and browser `qrcode` plus TypeScript definitions in `ui-shared`. No dependency enters `packages/engine`.
- **Encrypted persistence — approval pending.** Recommend save schema v2 with one-way v1 migration. Persist session/player bindings and replay counters, never live peer ids, one-time QR tokens, or phone keys. A phone reload re-pairs; an ordinary disconnect retains its in-memory key.
- **Package boundary — approval pending.** Recommend `packages/transport` (protocol/crypto, no engine or React), `packages/transport-webrtc` (trystero adapter), a host bridge in `ui-shared`, and a thin consumer in `ui-phone`. Only the host bridge may accept an engine ledger, and it passes already-scoped DTOs downward.
- **Confrontation ballot visibility — approval pending.** Recommend votes become public as cast, matching `sim-bot-policies.md`'s majority-so-far rule; only COMMS actions are sealed until close.

## Done when
- `docs/design/screens-v2.md` maps setup/deal, phone pairing, timed COMMS, private feedback, confrontation vote, disconnect fallback, and black-box verification
- each host/client message lists sender, receiver, visibility, encryption boundary, replay/idempotency rule, and disconnect behavior
- decisions above, any required fact-catalog-first commits, and the forced-vote prerequisite are recorded in this card

## Do not
- no code, dependency, schema, or Visibility change in this card
- do not design spoken-play capture or imply that encryption defeats a malicious host
