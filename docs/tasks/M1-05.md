---
id: M1-05
title: Incident frames: load, fire, innocent-twin, ambiguity gate, cooldowns
assignee: any implementer
spec_refs: ['Spec §8.2', 'Spec §8.3', 'Spec §9 pass 5']
invariants: ['INV-10']
---

# M1-05 — Incident frames: load, fire, innocent-twin, ambiguity gate, cooldowns

## Tests first
- property: twin-fired incidents — enumerator on the visible set never returns a unique actor
- unit: cooldown filtering and weight decay; recurrence counter
- integration: frame fires through interpreter, surfaces via renderer stub, cause facts land referee-scoped

## Done when
- frame loader per incident-content format (M1-11a defines files; this card defines code + a test fixture deck)
- claimant hook stubbed (agendas are M2) — twin path only

## Do not
- Identity-fields-reveal-last pricing goes in M1-07, not here
