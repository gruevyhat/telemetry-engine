---
id: M2-02
title: Retrofit existing referee draws to public commitments
assignee: any implementer
spec_refs: ['Spec §6', 'Spec §8', 'Spec §21.2']
invariants: ['INV-3', 'INV-6', 'INV-8']
---

# M2-02 — Retrofit existing referee draws to public commitments

## Tests first
- integration: every campaign commits exactly one `campaign.seedCommitted` fact before its first `secretRoll.committed` fact
- property: every existing reachable referee-scoped random draw has exactly one public verifying companion fact
- integration: kill/recreate before and after a draw matches uninterrupted facts and preimages byte-for-byte

## Done when
- campaign initialization commits M2-01's seed proposal through the phase interpreter before any draw; frame cause resolution, degradation/oracle secret paths, and market/world referee draws use the linked draw primitive through the same commit paths
- an explicit audit test fails when a new referee draw site omits a commitment

## Do not
- do not turn table/public rolls into secret rolls
- do not add another ledger append path or perturb unrelated named RNG streams
