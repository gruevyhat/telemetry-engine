---
id: M1-09
title: Template renderer, grammar helpers, TTS flag
assignee: any implementer
spec_refs: ['Spec §14', 'docs/design/maggie-voice.md']
invariants: ['INV-12']
---

# M1-09 — Template renderer, grammar helpers, TTS flag

## Tests first
- snapshot: every beat template × demo + trade fixture facts
- lint: TTS-safety (no markup/exclamations) over all template strings
- type-level: RenderedText is terminal — no API accepts it as input

## Done when
- Renderer interface + template backend; speechSynthesis behind a settings flag reading announce steps

## Do not
- No LLM backend (M5); voice-bible conformance is a review item — flag any line you author for reviewer attention
